<form action="/usuario/modificar" method="POST">
        {{#each usuarios}}
        <h1>Deseja Editar o usuário {{Nome}}?</h1>
            <input type="hidden" id="id" name="id" value="{{IdUsuario}}">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" class="form-control" value="{{Nome}}" required>
            <label for="email">E-mail: </label>
            <input type="email" id="email" name="email" class="form-control" value="{{Email}}" required>
            <br>
            <button type="submit" class="btn btn-success mt-2">Mandar</button>
            {{/each}}
            <a href="/usuario" class="btn btn-primary mt-2">Voltar</a>
        </form>